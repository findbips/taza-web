'use client';import {FormEvent,useEffect,useState} from 'react';import {api} from '../../lib/api';import Link from 'next/link';
type User={id:string;name:string;email:string;role:string};type Order={id:string;orderNumber:string,total:number,status:string,createdAt:string,items:{productName:string;quantity:number}[]};
export default function Account(){const [user,setUser]=useState<User|null>(null);const [orders,setOrders]=useState<Order[]>([]);const [mode,setMode]=useState<'login'|'register'>('login');const [error,setError]=useState('');const [form,setForm]=useState({name:'',email:'',password:'',phone:''});useEffect(()=>{api<{user:User}>('/api/auth/me').then(x=>{setUser(x.user);return api<{orders:Order[]}>('/api/orders/mine')}).then(x=>setOrders(x.orders)).catch(()=>{})},[]);async function submit(e:FormEvent){e.preventDefault();setError('');try{const x=await api<{user:User}>(`/api/auth/${mode==='login'?'login':'register'}`,{method:'POST',body:JSON.stringify(form)});setUser(x.user);if(x.user)api<{orders:Order[]}>('/api/orders/mine').then(y=>setOrders(y.orders))}catch(e){setError(e instanceof Error?e.message:'Please try again')}}async function logout(){await api('/api/auth/logout',{method:'POST'});setUser(null);setOrders([])}if(user)return <main><section className="pageTop"><div className="container"><div className="eyebrow">Your account</div><h1 className="display">Welcome, <em>{user.name.split(' ')[0]}.</em></h1><button className="btn" onClick={logout}>Sign out</button></div></section><section className="section" style={{paddingTop:20}}><div className="container"><h2 className="display" style={{fontSize:40}}>Order history</h2>{orders.length?<div className="tableWrap"><table className="table"><thead><tr><th>Order</th><th>Date</th><th>Items</th><th>Total</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.orderNumber}</td><td>{new Date(o.createdAt).toLocaleDateString()}</td><td>{o.items.map(i=>`${i.productName} × ${i.quantity}`).join(', ')}</td><td>৳ {o.total}</td><td>{o.status}</td></tr>)}</tbody></table></div>:<div className="empty">No orders yet. <Link href="/shop" style={{color:'var(--green)'}}>Explore TAZA →</Link></div>}</div></section></main>;return <main><section className="pageTop"><div className="container"><div className="eyebrow">TAZA account</div><h1 className="display">Keep your <em>TAZA</em> close.</h1></div></section><section className="section" style={{paddingTop:20}}><div className="container" style={{maxWidth:600}}><div className="storyPanel"><div className="filters"><button className={`filter ${mode==='login'?'active':''}`} onClick={()=>setMode('login')}>Sign in</button><button className={`filter ${mode==='register'?'active':''}`} onClick={()=>setMode('register')}>Create account</button></div><form className="form" onSubmit={submit}>{mode==='register'&&<label>Name<input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label>}<label>Email<input className="input" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label>{mode==='register'&&<label>Phone<input className="input" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label>}<label>Password<input className="input" type="password" required minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="error">{error}</div>}<button className="btn primary" type="submit">{mode==='login'?'Sign in':'Create account'}</button></form></div></div></section></main>}
