'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';
export default function Contact(){const [msg,setMsg]=useState('');const [form,setForm]=useState({name:'',email:'',phone:'',message:''});async function submit(e:FormEvent){e.preventDefault();setMsg('');try{await api('/api/contact',{method:'POST',body:JSON.stringify(form)});setMsg('Thanks. Your message has been received.');setForm({name:'',email:'',phone:'',message:''})}catch(e){setMsg(e instanceof Error?e.message:'Please try again.')}}return <main><section className="pageTop"><div className="container"><div className="eyebrow">Contact</div><h1 className="display">Let's talk about <em>TAZA.</em></h1><p className="lead">For product questions, partnerships, wholesale enquiries or anything else, send us a note.</p></div></section><section className="section"><div className="container split"><div><h2 className="display">Good conversations<br/>start here.</h2><p className="muted">hello@taza.bd</p><p className="muted">Bangladesh</p></div><form className="form" onSubmit={submit}><label>Name<input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input className="input" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>WhatsApp / phone<input className="input" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Message<textarea rows={6} required value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" type="submit">Send message</button></form></div></section></main>}
