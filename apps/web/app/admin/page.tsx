'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';
export default function Admin(){const [stats,setStats]=useState<any>(null);const [err,setErr]=useState('');useEffect(()=>{api('/api/admin/stats').then(setStats).catch(e=>setErr(e.message))},[]);return <main><div className="eyebrow">Command centre</div><h1 className="display">TAZA <em>overview.</em></h1>{err?<div className="error">{err}. Sign in as an admin first.</div>:stats?<div className="statGrid"><div className="stat"><small>Revenue</small><strong>৳ {stats.revenue.toLocaleString()}</strong></div><div className="stat"><small>Orders</small><strong>{stats.orders}</strong></div><div className="stat"><small>Customers</small><strong>{stats.customers}</strong></div><div className="stat"><small>Products</small><strong>{stats.products}</strong></div></div>:<div className="empty">Loading dashboard…</div>}<div className="storyPanel" style={{marginTop:25}}><div className="eyebrow">System</div><h2 style={{fontFamily:'Playfair Display',fontWeight:500}}>A premium storefront with a business-grade foundation.</h2><p className="muted">Next: payment gateway, image storage, delivery integration, audit logs, batch traceability and production deployment hardening.</p></div></main>}
