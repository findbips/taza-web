'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';export default function Subscribers(){const [rows,setRows]=useState<any[]>([]);const [err,setErr]=useState('');useEffect(()=>{api<{subscribers:any[]}>('/api/newsletter').then(x=>setRows(x.subscribers)).catch(e=>setErr(e.message))},[]);return <main><div className="eyebrow">Audience</div><h1 className="display">Early <em>list.</em></h1>{err&&<div className="error">{err}. Sign in as an admin first.</div>}<div className="tableWrap"><table className="table"><thead><tr><th>Email</th><th>Source</th><th>Joined</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.email}</td><td>{r.source||'website'}</td><td>{new Date(r.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
