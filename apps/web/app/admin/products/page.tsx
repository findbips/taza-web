'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';import type {Product} from '../../../lib/types';
export default function AdminProducts(){const [products,setProducts]=useState<Product[]>([]);const [err,setErr]=useState('');useEffect(()=>{api<{products:Product[]}>('/api/products').then(x=>setProducts(x.products)).catch(e=>setErr(e.message))},[]);return <main><div className="eyebrow">Catalog</div><h1 className="display">Products <em>& inventory.</em></h1>{err&&<div className="error">{err}</div>}<div className="tableWrap"><table className="table"><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.sku}</td><td>{p.category}</td><td>৳ {p.price}</td><td>{p.stock}</td></tr>)}</tbody></table></div></main>}
