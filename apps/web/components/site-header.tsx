'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';import {useCart} from './cart-provider';
export default function SiteHeader(){const path=usePathname();const {count}=useCart();return <header className="nav"><div className="container navin"><Link className="logo" href="/">TAZA<span>®</span></Link><nav className="links">{[['Shop','/shop'],['Our story','/about'],['Contact','/contact']].map(([t,h])=><Link className={path===h?'active':''} key={h} href={h}>{t}</Link>)}</nav><div className="navActions"><Link className="cartLink" href="/cart">Bag <b>{count}</b></Link><Link className="btn primary navCta" href="/shop">Explore TAZA</Link></div></div></header>}
