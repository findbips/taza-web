services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: taza
      POSTGRES_USER: taza
      POSTGRES_PASSWORD: taza_dev
    ports:
      - "5432:5432"
    volumes:
      - taza_postgres:/var/lib/postgresql/data
volumes:
  taza_postgres:
