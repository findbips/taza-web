'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';
export default function AdminOrders(){const [orders,setOrders]=useState<any[]>([]);const [err,setErr]=useState('');async function load(){try{setOrders((await api<{orders:any[]}>('/api/orders')).orders)}catch(e){setErr(e instanceof Error?e.message:'Unable to load')}}useEffect(()=>{load()},[]);async function status(id:string,status:string){await api(`/api/orders/${id}/status`,{method:'PATCH',body:JSON.stringify({status})});load()}return <main><div className="eyebrow">Commerce</div><h1 className="display">Order <em>desk.</em></h1>{err&&<div className="error">{err}. Sign in as an admin first.</div>}<div className="tableWrap"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Status</th><th>Update</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.orderNumber}<br/><span style={{color:'#697163'}}>{new Date(o.createdAt).toLocaleString()}</span></td><td>{o.customerName}<br/>{o.phone}</td><td>৳ {o.total}</td><td>{o.status}</td><td><select className="input" value={o.status} onChange={e=>status(o.id,e.target.value)}><option>PENDING</option><option>CONFIRMED</option><option>PROCESSING</option><option>SHIPPED</option><option>DELIVERED</option><option>CANCELLED</option></select></td></tr>)}</tbody></table></div></main>}
