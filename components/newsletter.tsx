'use client';import {FormEvent,useState} from 'react';import {api} from '../lib/api';
export default function Newsletter(){const [email,setEmail]=useState('');const [state,setState]=useState('');async function submit(e:FormEvent){e.preventDefault();setState('');try{await api('/api/newsletter',{method:'POST',body:JSON.stringify({email,source:'taza.bd'})});setState('✓ You’re on the list.');setEmail('')}catch(err){setState(err instanceof Error?err.message:'Try again.')}}return <form className="newsletter" onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address"/><button className="btn primary" type="submit">Notify me</button>{state&&<span>{state}</span>}</form>}
